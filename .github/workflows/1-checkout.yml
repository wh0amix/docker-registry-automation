name: "1. Checkout Code"

on:
  workflow_dispatch:

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Verification du checkout
        run: |
          echo "✓ Code checked out successfully"
          ls -la
